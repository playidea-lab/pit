[project]
name = "pit"
version = "0.1.0"
description = "Product / Idea Tracker - 기획/아이디어/결정/체크리스트를 Git처럼 버전 관리하고, git의 실제 코드와 비교하여 정합도를 평가하는 PM용 관제 시스템"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [
    { name = "changmin", email = "changmin@playidea.io" }
]
keywords = ["cli", "project-management", "version-control", "planning"]

dependencies = [
    "typer>=0.9.0",
    "rich>=13.0.0",
    "pydantic>=2.0.0",
    "pyyaml>=6.0",
    "anthropic>=0.75.0",
    "python-dotenv>=1.2.1",
    "mcp>=1.23.3",
    "httpx>=0.28.1",
]

[project.optional-dependencies]
server = [
    "fastmcp>=2.12",
]

[project.scripts]
pit = "pit.cli.main:app"
pit-mcp = "pit.mcp_server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["pit"]
# 추출·트윈 프롬프트는 코드가 아니라 데이터다. 빠지면 실행 시점에야 터지므로 명시한다.
artifacts = ["pit/twin/prompts/*.md"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
markers = [
    "slow: wheel 빌드처럼 오래 걸리는 테스트",
    "integration: 로컬 Supabase 등 외부 서비스가 필요한 테스트",
]

[dependency-groups]
dev = [
    "psycopg[binary]>=3.2",
    "pytest>=9.0.2",
    "ruff>=0.14.9",
]
